import React from "react";
import { createRoot } from "react-dom/client";
import SwipeKitOut from "./SwipeTheFlat.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SwipeKitOut />
  </React.StrictMode>
);
